# 환경 변수 템플릿
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값을 설정하세요
# cp env.example .env

# 데이터베이스 설정
POSTGRES_USER=storeuser
POSTGRES_PASSWORD=storepass
POSTGRES_DB=storemanagement
POSTGRES_PORT=5432

# Redis 설정
REDIS_PORT=6379

# Node.js 환경
NODE_ENV=development

# 서비스별 데이터베이스 URL (필요시 개별 설정)
ATTENDANCE_DATABASE_URL=postgresql://storeuser:storepass@postgres:5432/storemanagement
INVENTORY_DATABASE_URL=postgresql://storeuser:storepass@postgres:5432/storemanagement
SALES_DATABASE_URL=postgresql://storeuser:storepass@postgres:5432/storemanagement

# Gateway 서비스 설정
JWT_SECRET=your-secret-key-change-in-production
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Frontend 설정
VITE_API_URL=http://localhost:4000/graphql
FRONTEND_PORT=5173

